# Chistovye - Веб-приложение каталога товаров

Полнофункциональное веб-приложение на Nuxt 3 (Frontend) и Django (Backend) с адаптивным дизайном, анимациями и админ-панелью.

## 🚀 Возможности

### Frontend (Nuxt 3)
- ✨ Современный одностраничный интерфейс (SPA)
- 🎨 Адаптивный дизайн (Desktop/Tablet/Mobile)
- 🎭 Плавные анимации (GSAP, Vue Transitions)
- 🎪 Карусели товаров и баннеров (Swiper.js)
- 🎯 TailwindCSS для стилизации
- 📱 Полная отзывчивость

### Backend (Django + DRF)
- 🔐 JWT авторизация
- 📦 REST API для товаров, категорий, баннеров
- 🖼️ Загрузка изображений
- 🗄️ PostgreSQL база данных
- 📚 Автоматическая документация API (Swagger/ReDoc)
- 🛡️ CORS настройка

### Админ-панель
- ✏️ CRUD операции для товаров
- 📂 Управление категориями
- 🎨 Управление баннерами
- 🏷️ Метки New/Sale
- 📸 Загрузка множественных изображений

## 📋 Требования

### Backend
- Python 3.10+
- PostgreSQL 13+
- pip

### Frontend
- Node.js 18+
- npm или yarn

## 🛠️ Установка и запуск

### Backend (Django)

1. **Перейдите в папку backend:**
```bash
cd backend
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
```

3. **Активируйте виртуальное окружение:**
- Windows:
```bash
venv\Scripts\activate
```
- Linux/Mac:
```bash
source venv/bin/activate
```

4. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

5. **Создайте файл .env:**
```bash
cp .env.example .env
```

Отредактируйте `.env` файл с вашими настройками:
```env
SECRET_KEY=your-secret-key-here
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=chistovye_db
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

Если вы оставите переменные пустыми, backend по умолчанию возьмет `DB_NAME=chistovye`, `DB_USER=postgres`, `DB_PASSWORD=postgres`, `DB_HOST=localhost`, `DB_PORT=5432` (см. `config/settings.py`).

6. **Создайте базу данных PostgreSQL:**
```sql
CREATE DATABASE chistovye_db;
```

7. **Выполните миграции:**
```bash
python manage.py makemigrations
python manage.py migrate
```

8. **Создайте суперпользователя:**
```bash
python manage.py createsuperuser
```

9. **Соберите статические файлы:**
```bash
python manage.py collectstatic --noinput
```

10. **Запустите сервер:**
```bash
python manage.py runserver
```

Backend будет доступен на `http://localhost:8000`

- API: `http://localhost:8000/api/`
- Админ-панель: `http://localhost:8000/admin/`
- Swagger: `http://localhost:8000/swagger/`
- ReDoc: `http://localhost:8000/redoc/`

### Frontend (Nuxt 3)

1. **Перейдите в папку frontend:**
```bash
cd frontend
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Создайте файл .env (опционально):**
```bash
NUXT_PUBLIC_API_BASE=http://localhost:8000/api
```

4. **Запустите dev-сервер:**
```bash
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

## 📡 API Endpoints

### Products
- `GET /api/products/` - Список товаров
- `GET /api/products/{slug}/` - Детали товара
- `GET /api/products/id/{id}/` - Детали товара по id
- `POST /api/products/` - Создать товар (требуется авторизация)
- `PUT /api/products/{slug}/` - Обновить товар (требуется авторизация)
- `DELETE /api/products/{slug}/` - Удалить товар (требуется авторизация)
- `GET /api/products/new_products/` - Новинки
- `GET /api/products/sale_products/` - Акции

### Categories
- `GET /api/categories/` - Список категорий
- `GET /api/categories/{slug}/` - Детали категории
- `GET /api/categories/id/{id}/` - Детали категории по id
- `POST /api/categories/` - Создать категорию (требуется авторизация)
- `PUT /api/categories/{slug}/` - Обновить категорию (требуется авторизация)
- `DELETE /api/categories/{slug}/` - Удалить категорию (требуется авторизация)

### Banners
- `GET /api/promos/` - Список промоций
- `GET /api/promos/{id}/` - Детали промоции
- `POST /api/promos/` - Создать промоцию (требуется авторизация)
- `PUT /api/promos/{id}/` - Обновить промоцию (требуется авторизация)
- `DELETE /api/promos/{id}/` - Удалить промоцию (требуется авторизация)

### Authentication
- `POST /api/token/` - Получить JWT токен
- `POST /api/token/refresh/` - Обновить JWT токен

## 🎨 Структура проекта

```
chistovye/
├── backend/
│   ├── config/              # Настройки Django
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── ...
│   ├── products/            # Приложение товаров
│   │   ├── models.py        # Модели БД
│   │   ├── serializers.py   # DRF сериализаторы
│   │   ├── views.py         # API views
│   │   ├── admin.py         # Админ-панель
│   │   └── urls.py          # URL маршруты
│   ├── media/               # Загруженные файлы
│   ├── manage.py
│   └── requirements.txt
│
└── frontend/
    ├── assets/              # Статические ресурсы
    │   └── css/
    ├── components/          # Vue компоненты
    │   ├── TheHeader.vue
    │   ├── TheFooter.vue
    │   ├── ProductCard.vue
    │   ├── BannerCarousel.vue
    │   └── ProductCarousel.vue
    ├── composables/         # Composables
    │   └── useApi.js
    ├── layouts/             # Layouts
    │   └── default.vue
    ├── pages/               # Страницы
    │   ├── index.vue        # Главная
    │   ├── about.vue        # О нас
    │   └── products/
    │       └── [slug].vue   # Детали товара
    ├── stores/              # Pinia stores
    │   └── products.js
    ├── nuxt.config.ts       # Конфигурация Nuxt
    ├── tailwind.config.js   # Конфигурация Tailwind
    └── package.json
```

## 🎯 Использование админ-панели

1. Войдите в админ-панель: `http://localhost:8000/admin/`
2. Используйте учетные данные суперпользователя

### Управление товарами:
- Добавьте категорию через "Категории"
- Создайте товар через "Товары"
- Загрузите изображения через inline-форму
- Установите метки New/Sale
- Укажите цены (price, old_price)

### Управление баннерами:
- Создайте баннер через "Баннеры"
- Загрузите изображение
- Укажите заголовок и ссылку (опционально)
- Установите порядок отображения

## 🚀 Production

### Backend

1. **Установите переменные окружения:**
```env
DEBUG=False
ALLOWED_HOSTS=yourdomain.com
SECRET_KEY=strong-random-secret-key
```

2. **Соберите статику:**
```bash
python manage.py collectstatic
```

3. **Используйте Gunicorn:**
```bash
pip install gunicorn
gunicorn config.wsgi:application --bind 0.0.0.0:8000
```

### Frontend

1. **Соберите приложение:**
```bash
npm run build
```

2. **Запустите production сервер:**
```bash
npm run preview
```

Или используйте Nginx для раздачи статики.

## 📞 Контакты

- Телефон: +7 701 263 50 25
- Адрес: Пичугина 162

## 📝 Лицензия

© 2025 Chistovye. Все права защищены®

---

**Разработано с ❤️ для качественного онлайн-каталога**
