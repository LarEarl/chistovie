# 🎬 ПЕРВЫЙ ЗАПУСК - Пошаговая инструкция

## ⚡ Самый быстрый способ (Docker)

Если у вас установлен Docker:

```bash
# 1. Перейдите в папку проекта
cd c:\Users\Lar\Desktop\project\chistovye

# 2. Запустите всё одной командой
docker-compose up -d

# 3. Создайте суперпользователя
docker-compose exec backend python manage.py createsuperuser

# 4. Загрузите тестовые данные
docker-compose exec backend python manage.py shell < load_sample_data.py
```

✅ Готово! Откройте:
- Frontend: http://localhost:3000
- Backend: http://localhost:8000
- Админ: http://localhost:8000/admin

---

## 🔧 Ручная установка (без Docker)

### Шаг 1: Установите зависимости

**Требования:**
- Python 3.10+
- Node.js 18+
- PostgreSQL 13+

### Шаг 2: Настройте PostgreSQL

```sql
-- Откройте pgAdmin или psql и выполните:
CREATE DATABASE chistovye_db;
CREATE USER chistovye_user WITH PASSWORD 'password123';
ALTER ROLE chistovye_user SET client_encoding TO 'utf8';
ALTER ROLE chistovye_user SET default_transaction_isolation TO 'read committed';
ALTER ROLE chistovye_user SET timezone TO 'Asia/Almaty';
GRANT ALL PRIVILEGES ON DATABASE chistovye_db TO chistovye_user;
```

### Шаг 3: Настройте Backend

```powershell
# Перейдите в папку backend
cd backend

# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
.\venv\Scripts\Activate.ps1

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
Copy-Item .env.example .env

# Отредактируйте .env (откройте в блокноте)
notepad .env
```

**В файле .env замените:**
```env
SECRET_KEY=django-insecure-your-secret-key-change-this
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=chistovye_db
DB_USER=chistovye_user
DB_PASSWORD=password123
DB_HOST=localhost
DB_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

```powershell
# Выполните миграции
python manage.py makemigrations
python manage.py migrate

# Создайте суперпользователя
python manage.py createsuperuser
# Введите: username, email (можно пропустить), password

# Загрузите тестовые данные
python manage.py shell < load_sample_data.py

# Запустите сервер
python manage.py runserver
```

✅ Backend запущен на http://localhost:8000

### Шаг 4: Настройте Frontend

**Откройте НОВЫЙ терминал PowerShell:**

```powershell
# Перейдите в папку frontend
cd frontend

# Установите зависимости
npm install

# Создайте .env (опционально)
echo "NUXT_PUBLIC_API_BASE=http://localhost:8000/api" > .env

# Запустите dev-сервер
npm run dev
```

✅ Frontend запущен на http://localhost:3000

---

## 📊 Добавьте данные через админку

1. **Откройте:** http://localhost:8000/admin
2. **Войдите** с данными суперпользователя
3. **Добавьте изображения:**
   - Перейдите в "Товары"
   - Откройте любой товар
   - Прокрутите вниз до "Изображения товара"
   - Нажмите "Добавить ещё Изображение товара"
   - Загрузите изображение
   - Сохраните

4. **Создайте баннеры:**
   - Перейдите в "Баннеры"
   - Нажмите "Добавить баннер"
   - Заполните название
   - Загрузите изображение (рекомендуемый размер: 1920x600px)
   - Отметьте "Активен"
   - Сохраните

---

## 🧪 Проверьте работу

### Frontend (http://localhost:3000)
- [ ] Открывается главная страница
- [ ] Видны баннеры (если загрузили)
- [ ] Отображаются товары
- [ ] Работает фильтрация по категориям
- [ ] Можно открыть детальную страницу товара
- [ ] Работает страница "О нас"

### Backend (http://localhost:8000)
- [ ] Открывается главная страница API
- [ ] Доступна админка /admin
- [ ] Swagger UI: /swagger
- [ ] ReDoc: /redoc

### API
Проверьте в браузере:
- http://localhost:8000/api/products/
- http://localhost:8000/api/categories/
- http://localhost:8000/api/banners/

---

## 🐛 Решение проблем

### Backend не запускается

**Ошибка подключения к БД:**
```powershell
# Проверьте, что PostgreSQL запущен
Get-Service -Name postgresql*

# Если не запущен:
Start-Service -Name postgresql-x64-15  # версия может отличаться
```

**Ошибка: No module named 'products'**
```powershell
# Убедитесь, что вы в папке backend и venv активирован
cd backend
.\venv\Scripts\Activate.ps1
```

### Frontend не запускается

**Ошибка: Cannot find module**
```powershell
# Удалите node_modules и переустановите
Remove-Item -Recurse -Force node_modules
Remove-Item -Force package-lock.json
npm install
```

**Порт 3000 занят:**
```powershell
# Используйте другой порт
npm run dev -- --port 3001
```

### API не работает на Frontend

**CORS ошибки:**
1. Проверьте .env в backend
2. Убедитесь что CORS_ALLOWED_ORIGINS содержит http://localhost:3000
3. Перезапустите backend

---

## 📚 Что дальше?

1. **Изучите документацию:**
   - [README.md](./README.md) - Полное описание
   - [API_DOCS.md](./API_DOCS.md) - API документация
   - [FEATURES.md](./FEATURES.md) - Список возможностей

2. **Добавьте свои данные:**
   - Создайте категории
   - Добавьте товары с описаниями
   - Загрузите качественные изображения
   - Создайте красивые баннеры

3. **Кастомизируйте дизайн:**
   - Измените цвета в `frontend/tailwind.config.js`
   - Обновите логотип в `TheHeader.vue`
   - Добавьте свои контакты в `TheFooter.vue`

4. **Готовьтесь к продакшену:**
   - Прочитайте [DEPLOYMENT.md](./DEPLOYMENT.md)
   - Настройте production переменные
   - Выберите хостинг

---

## 🎉 Поздравляем!

Проект готов к работе! Если возникнут вопросы:
1. Проверьте документацию
2. Посмотрите логи (консоль браузера, терминал)
3. Убедитесь, что все сервисы запущены

**Удачи в разработке! 🚀**
