# Быстрый старт проекта Chistovye

## Пошаговая инструкция для запуска проекта

### Шаг 1: Установка PostgreSQL

1. Скачайте и установите PostgreSQL с официального сайта
2. Запустите pgAdmin или psql
3. Создайте базу данных:
```sql
CREATE DATABASE chistovye_db;
CREATE USER chistovye_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE chistovye_db TO chistovye_user;
```

### Шаг 2: Настройка Backend

```bash
# 1. Перейдите в папку backend
cd backend

# 2. Создайте виртуальное окружение
python -m venv venv

# 3. Активируйте виртуальное окружение
# Windows PowerShell:
venv\Scripts\Activate.ps1
# Windows CMD:
venv\Scripts\activate.bat

# 4. Установите зависимости
pip install -r requirements.txt

# 5. Скопируйте .env.example в .env
Copy-Item .env.example .env

# 6. Отредактируйте .env файл с вашими данными

# 7. Выполните миграции
python manage.py makemigrations
python manage.py migrate

# 8. Создайте суперпользователя
python manage.py createsuperuser
# Введите username, email (опционально), password

# 9. Запустите сервер
python manage.py runserver
```

Backend запущен на: http://localhost:8000

### Шаг 3: Настройка Frontend

```bash
# 1. Откройте новый терминал и перейдите в папку frontend
cd frontend

# 2. Установите зависимости
npm install

# 3. Запустите dev сервер
npm run dev
```

Frontend запущен на: http://localhost:3000

### Шаг 4: Добавление тестовых данных

1. Откройте http://localhost:8000/admin/
2. Войдите с данными суперпользователя
3. Создайте несколько категорий
4. Добавьте товары с изображениями
5. Создайте баннеры для главной страницы

### Проверка работы

1. Откройте http://localhost:3000
2. Проверьте отображение баннеров
3. Проверьте каталог товаров
4. Перейдите на страницу "О нас"
5. Кликните на товар для просмотра деталей

## Возможные проблемы и решения

### Backend

**Проблема:** Ошибка подключения к базе данных
**Решение:** 
- Проверьте, что PostgreSQL запущен
- Проверьте данные в .env файле
- Убедитесь, что база данных создана

**Проблема:** ModuleNotFoundError
**Решение:**
- Убедитесь, что виртуальное окружение активировано
- Переустановите зависимости: `pip install -r requirements.txt`

**Проблема:** Изображения не загружаются
**Решение:**
- Проверьте настройку MEDIA_URL и MEDIA_ROOT в settings.py
- Убедитесь, что папка media существует

### Frontend

**Проблема:** API запросы не работают
**Решение:**
- Проверьте, что backend запущен на порту 8000
- Проверьте CORS настройки в Django
- Проверьте apiBase в nuxt.config.ts

**Проблема:** npm install ошибки
**Решение:**
- Обновите Node.js до версии 18+
- Очистите cache: `npm cache clean --force`
- Удалите node_modules и package-lock.json, установите заново

**Проблема:** Компоненты не отображаются
**Решение:**
- Проверьте консоль браузера на ошибки
- Убедитесь, что все зависимости установлены
- Перезапустите dev сервер

## Полезные команды

### Backend
```bash
# Создать новые миграции
python manage.py makemigrations

# Применить миграции
python manage.py migrate

# Создать суперпользователя
python manage.py createsuperuser

# Собрать статику
python manage.py collectstatic

# Запустить shell
python manage.py shell
```

### Frontend
```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для production
npm run build

# Запуск production preview
npm run preview

# Очистка
rm -rf node_modules package-lock.json
npm install
```

## Тестирование API

### Через браузер:
- Swagger UI: http://localhost:8000/swagger/
- ReDoc: http://localhost:8000/redoc/

### Через curl (PowerShell):

```powershell
# Получить список товаров
curl http://localhost:8000/api/products/

# Получить список категорий
curl http://localhost:8000/api/categories/

# Получить баннеры
curl http://localhost:8000/api/banners/

# Получить JWT токен
curl -X POST http://localhost:8000/api/token/ `
  -H "Content-Type: application/json" `
  -d '{"username":"admin","password":"your_password"}'
```

## Структура URL

### Frontend:
- Главная: http://localhost:3000/
- О нас: http://localhost:3000/about
- Товар: http://localhost:3000/products/{slug}

### Backend:
- API: http://localhost:8000/api/
- Админ: http://localhost:8000/admin/
- Документация: http://localhost:8000/swagger/

---

После выполнения всех шагов проект готов к работе! 🎉
